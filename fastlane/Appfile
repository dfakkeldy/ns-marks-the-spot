# Appfile — shared app metadata for all lanes

app_identifier "Dan.ns-marks-the-spot" # The bundle identifier of your app
apple_id ""                            # Your Apple ID email (optional — only needed for App Store operations)
team_id "8AXNA5B37R"                   # Developer Portal Team ID

# For now, we use automatic code signing.
# When you add an Apple ID above, fastlane can manage certs/profiles.
