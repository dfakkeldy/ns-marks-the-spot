# Judique boundary and additional checks

Freeze the existing 39-control TPS from visual-expansion/sheet-observations.json
(SHA256 632f4b19fcbe64384232fc03693c4f51f646e51332c3d56e382e977ff0afda04).
No controls or original checks change in this stage. Trace a map-content mask in
the native 10815×7549 scan frame; keep the source raster and control coordinates
unchanged. The inner printed neatline defines map content, not geographic accuracy.

Select eight additional identifiable physical features across northern/southern
edges, eastern edges, and inland terrain, excluding existing controls and checks.
Inspect native crosshairs and modern branch topology, record source/modern feature
identities, then freeze these observations before calculating TPS residuals.
Use the preceding affine search guide (15 saved hand controls + 12 local controls),
not the evaluated TPS, to render search context. This remains same-agent assisted
annotation with prior sheet exposure, not independent survey or pristine blind QA.
Keep rejected/ambiguous searches and geographic gaps visible.

Use the established median ≤100 m / worst ≤200 m ground-error limits for this
new set. Report inside/outside the existing control hull separately; test sampled
folds in the proposed crop. Do not silently relax the limits, add these checks
to the fit, or repair a match just to improve the score. A failure triggers a
reported diagnosis and a separate revision if justified by source evidence.

Render a neatline-masked full-sheet diagnostic and a conservative supported-area
preview (intersection with the existing control hull). A crop cannot validate
extrapolated edges or make neighbouring sheets agree. Identify adjacent sheets
from the printed index/verified catalogue, and carry unresolved joins into the
mosaic plan. No production control replacement, tile publishing or deployment.
