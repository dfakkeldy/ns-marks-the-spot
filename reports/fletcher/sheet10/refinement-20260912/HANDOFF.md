# Sheet 10 continuation

Keep the PR draft; do not replace the old raster with this experiment. No merge/deploy. One V04→C15 promotion fits locally but worsens downstream/interior geometry. Preserve all twelve old controls, their history and the failed thirteen-control result.

Before another repair, trace the entire Stillwater–North Branch–Baddeck Lakes system and main-channel versus tributary ordering from original native imagery and modern geometry. Post-score F01 identity remains unresolved; its original and corrected coordinates and 622.78/894.14 m results must remain unchanged. Any revised interpretation is diagnostic. V01/Q02 also require stronger topology evidence, not residual-based snapping. Eastern shore identity and distributed checks remain missing. All Sheet 08/11/12 seams remain unaccepted despite completing the previously missing northern review.

Use frozen JSONs with `reports/fletcher/full-sheets/score.py` and `render.py`, with GDAL `/opt/local/bin` on PATH and the benchmark Python. Render into a new external directory with the original native PNG, complete `../boundary.json` and separate checks. Never overwrite existing experiments. `freeze.py` refuses to overwrite the freeze. `export_csv.py`, `verify_import.ts`, point/warp/join scripts and `verify-browser.mjs` retain reproduction logic. `verify_packet.py` checks the recorded report and external files without asserting geography.

Native and modern data remain in `~/Downloads/fletcher-sheet10/native/` and `reference-full/`. The failed experiment raster/browser are in `~/Downloads/fletcher-sheet10/refinement-20260912/`; hashes are in README and receipts. Old twelve-control raster remains in `regional-twelve/`.

Queue: separate Sheet 12/13 drafts are published; continue neighboring inadequate sheets from current origin/nightly. Preserve earlier accepted work and all prior evidence. No DeepSeek, subagents, automations, goals, Apple changes, merges or deployments.
