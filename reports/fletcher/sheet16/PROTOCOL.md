# Route 19 first: Sheet 16 north of Judique

Prioritize a continuous Port Hawkesbury–Judique–Port Hood–Mabou browsing corridor.
This pass moves north from Judique onto Sheet 16. Preserve Sheet 22's existing
failed diagnostic, without spending this pass extending its eastern coverage.

Preserve the saved Sheet 16 annotation (32 rows) and repository export (38 rows).
The annotation corroborates the first 32 export rows at higher precision. Use
those exact values where available, retaining the six later gcp rows at their
saved precision. The primary baseline contains all 32 gcp-* rows. Keep the six
candidate-prefixed rows separate until their physical identities are reviewed.
Do not call legacy candidate rows hand-verified ground truth merely because an
old CSV header described all rows as measured by hand.

Acquire native 10822×7531 IIIF regions; the old benchmark JPEG is degraded.
Use a simple fit only to guide searches, and inspect native crosshairs and
modern water/road/shore topology before admitting points. Prioritize western
coastal and inland features around Route 19, plus the shared southern edge.
Preserve existing hand coordinates; document any necessary model revision.

Freeze at least eight separate physical checks before comparing affine and TPS.
For this adjacent 1:63,360 sheet serving the same browsing corridor, retain the
predeclared project limits: median ≤100 m and worst ≤200 m ground error.
Report corridor coverage separately from untested sheet areas. Failed checks
remain visible; model selection or repair consumes their independence. Use new
checks to validate any repair informed by the first set.

Deliver a reversible native-content mask and supported diagnostic or accepted
preview as evidence permits. Compare the southern join with Judique without
forcing agreement to an unverified neighbouring edge. Verify actual NSMtS import,
render and save/reload. Keep large rasters local and publish the report in a PR
to nightly. Geographic acceptance is separate from passing code or browser tests.
No live layer replacement or final tile publication is part of this pass.
