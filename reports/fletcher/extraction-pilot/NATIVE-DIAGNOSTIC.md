# Source-resolution diagnostic declared before scoring

Rerun the previously frozen Sheet 19 primary and wide-window matchers unchanged
against a mosaic from native 1536-pixel IIIF regions. This isolates source quality
from the proposed semantic-extraction design. It is a post-hoc diagnostic using
previously inspected controls, not a new blind evaluation or production warp.
Freeze both prediction files before scoring. Preserve all old image hashes,
inputs, proposals and results. The regional mosaic must match scan dimensions
and receive source/hash receipts. The older bulk-download image is retained for
paired visual comparison. No Sheet 16 claim is revised without a corresponding
native rerun.
