# Sheet 22 / Hawkesbury georeferencing and northern join

Start from the user's latest saved August 2 export, corroborated by its matching
IIIF georeferencing annotation. Preserve all 17 manually added `gcp-*` rows
exactly. Preserve the original export separately, including `corner-se`, but
exclude that remaining default corner from the physical-feature draft.

Use a verified native 10790×7687 scan of Rumsey item RUMSEY~8~1~2647~290015.
Use the saved manual controls' affine fit only as a search guide. Inspect physical
features and native crosshairs before assigning matches. The old repository
printed-grid placement is suspect and must not be used as geographic truth.

Add distributed controls where needed, particularly the northern join and
southern/eastern limits. Reserve at least eight additional physical-feature
checks across available land coverage. Freeze identities, pixels, roles and
modern reference coordinates before scoring. Evaluate affine and TPS fits on
the same set, disclosing any use of those checks for model selection or repair.
Keep rejected or changed matches and failed scores, rather than optimizing
observations to make a gate pass.

For this adjacent 1:63,360 sheet intended for the same browsing layer, retain
Judique's median ≤100 m and worst ≤200 m ground-error limits. This is a project
browsing threshold, not survey accuracy. Report control-hull coverage and sampled
folds separately. An isolated passing patch cannot establish whole-sheet accuracy.

Trace a reversible native map-content boundary. Preserve scan/control coordinates.
Clip the usable preview to supported coverage. Inspect shared features at the
northern join against Judique's frozen draft; its known southern lake mismatch
must remain visible. Do not force Sheet 22 to inherit Judique's unverified edge.
Report actual overlap or gaps before proposing a final mosaic seam.

Deliver controls/checks, diagnostic evidence, reproducible scores and a masked
draft GeoTIFF. Test the actual NSMtS import/render path for that artifact where
available. Keep large sources/rasters local. No production layer replacement,
tile publication or deployment is authorized by this experiment.
