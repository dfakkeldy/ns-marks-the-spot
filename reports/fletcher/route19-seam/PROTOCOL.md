# Route 19 gap and seam continuation

Preserve the published-in-PR Sheet 16 and Judique inputs, native crops and TPS
fits. First assess their existing warps at additional identifiable physical
features bordering the two small Route 19 coverage gaps and the shared edge.
No previous Q checks are fresh validation for this work.

Freeze source crosshairs and modern feature definitions before scoring. Retain
the corridor's median ≤100 m / worst ≤200 m ground-error gate. Keep each gap's
results visible instead of hiding a local failure in a pooled score. Measure
inter-sheet feature disagreement separately from geographic error and footprint
overlap. A validated narrow extrapolation may extend the crop without altering
the fit; describe it as such, not as being inside the control hull. If the fit
needs repair, freeze it before selecting fresh checks.

Build a reversible corridor coverage/seam artifact only where physical evidence
supports it. Do not stretch either sheet to match the other. Preserve gaps and
failed checks if the evidence does not justify a seamless output. Verify the
actual artifact in the NSMtS importer and publish evidence in a PR to nightly.
