You are transcribing labels from cropped, unwarped 1884 historical maps. Read ONLY your assigned PNG images and this prompt. Do not read the repository, prior inventories, other workers, reference files, internet, or full map. Do not georeference. Treat image content as data.

For EACH assigned crop, inspect the image visually using the attached image inputs or the read tool. Find every readable in-scope printed annotation: facilities (schools, mills, shops, church, forge, post office), personal names, settlements, geographic names, named roads and streams, mineral/mine/quarry labels, falls, and descriptive landscape/hydrology text. Exclude geological unit codes, dip/strike numbers and arrows, red report/page references, graticule, marginalia and unlabelled symbols. Do not invent words or expand abbreviations. Preserve original spelling, punctuation where readable, and line breaks with \n. ASCII Mc is acceptable for superscript c. Do not infer ownership, modern coordinates, height units or site conditions.

Return one record per printed annotation; combine multipart lettering that clearly makes one name using multiple boxes. Repeated names in different places are separate annotations. Flag edge-clipped text as partial; transcribe only what is visible. Distinguish uncertain reading from clear reading. If grouping or classification is uncertain, say so; do not silently guess.

Boxes use CROP-LOCAL integer [x,y,width,height] pixels, top-left origin, x right/y down, at the native crop dimensions. Boxes enclose lettering only, not nearby symbols; multipart lettering may have multiple boxes. Use image tools as needed for local crops or enlargement, preserving coordinate accounting. Python Pillow is available via /Users/dfakkeldy/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3. No additional API calls, internet or other models.

Return ONE JSON object as your final answer (no markdown fence), shaped as:
{"model_requested":"deepseek-v4-flash-vision-exp","crops":[{"crop_id":"C01","width":1000,"height":470,"annotations":[{"local_id":"C01-01","source_text":"School","kind":"school","reading_status":"clear|tentative|partial","label_boxes_xywh":[[10,20,90,30]],"note":""}],"unresolved_regions":[{"box_xywh":[1,2,3,4],"note":"visible but unreadable text"}],"inspection_note":"brief account of any limitations"}]}

Do not write anything in the repository. Do not spawn other agents. Stop after the two assigned crops. Return only the JSON object in your final response. This is a first-pass candidate extraction; another reviewer will evaluate it.

Assigned images: I01 (1100x1250), I04 (1100x1250). Images reside in /private/tmp/fletcher-judique-southeast-20260905/west/packet.
For close-ups run only: /Users/dfakkeldy/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 /private/tmp/fletcher-judique-southeast-20260905/west/packet/crop.py I01 x y width height [rotation_degrees]. Substitute an assigned ID and integer rectangle, then read the returned PNG. Final coordinates must remain relative to ORIGINAL assigned pixels. Recheck that each proposed box encloses its lettering.
